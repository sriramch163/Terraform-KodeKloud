# Create EC2 Instance Using Terraform

## 🎯 Objective

Provision an EC2 instance named devops-ec2 in the us-east-1 AWS region using Terraform.
The instance should meet the following configuration requirements:

- **Operating System**: Amazon Linux (AMI ID: ami-0c101f26f147fa7fd)
- **Instance Type**: t2.micro
- **Key Pair**: Create a new RSA key named devops-kp
- **Security Group**: Attach the default security group automatically

## 📋 Overview

This project demonstrates the provisioning of an AWS EC2 instance using Terraform Infrastructure as Code (IaC). The configuration creates a complete compute environment including the instance, SSH key pair, and security configurations suitable for development and testing workloads.

## 🏗️ Architecture

- **Compute**: EC2 t2.micro instance (1 vCPU, 1 GB RAM)
- **Operating System**: Amazon Linux 2023
- **Region**: us-east-1 (N. Virginia)
- **Availability Zone**: Automatically assigned by AWS
- **Key Management**: RSA 4096-bit SSH key pair
- **Network Security**: Default VPC security group

## 📁 Directory Structure

```
Create EC2 Instance Using Terraform/
├── main.tf          # Main Terraform configuration
├── README.md        # Project documentation
└── Task.png         # Task reference image
```

## 🔧 Prerequisites

- AWS CLI configured with appropriate credentials
- Terraform installed (version 0.12+)
- AWS account with EC2 and key pair creation permissions
- Basic understanding of SSH key pairs for instance access

## 🚀 Usage

1. **Initialize Terraform**:
   ```bash
   terraform init
   ```

2. **Validate configuration**:
   ```bash
   terraform validate
   ```

3. **Plan the deployment**:
   ```bash
   terraform plan
   ```

4. **Apply the configuration**:
   ```bash
   terraform apply
   ```

5. **View instance details**:
   ```bash
   terraform show
   ```

6. **Get instance outputs**:
   ```bash
   terraform output
   ```

7. **Clean up resources**:
   ```bash
   terraform destroy
   ```

## 📊 Expected Outputs

After successful deployment, you will have:
- EC2 instance named "devops-ec2" running Amazon Linux
- RSA SSH key pair "devops-kp" for secure access
- Instance configured with default security group
- Public and private IP addresses assigned
- Instance ready for SSH connection

## 💡 Key Features

- **Automated Key Generation**: Creates RSA 4096-bit key pair automatically
- **Secure Access**: SSH key-based authentication for instance access
- **Cost Effective**: Uses t2.micro instance (eligible for AWS Free Tier)
- **Default Networking**: Leverages default VPC and security group
- **Resource Tagging**: Proper naming convention for resource identification
- **Regional Deployment**: Targeted deployment in us-east-1 region

## 🔍 Verification

To verify the EC2 instance creation:
1. **AWS Console**: Navigate to EC2 → Instances
2. **Instance Name**: Look for "devops-ec2" in the Name tag
3. **Instance State**: Confirm status shows "running"
4. **Key Pair**: Verify "devops-kp" is listed in Key Pairs section
5. **Region**: Ensure deployment is in us-east-1
6. **AMI**: Confirm AMI ID matches ami-0c101f26f147fa7fd

## 🔐 SSH Access

To connect to your instance:
1. Save the private key from Terraform state (if needed)
2. Set appropriate permissions: `chmod 400 private-key.pem`
3. Connect via SSH: `ssh -i private-key.pem ec2-user@<public-ip>`

## ⚠️ Important Notes

- The private key is generated by Terraform and stored in state file
- Ensure proper backup of Terraform state for key recovery
- Default security group may have restrictive access rules
- t2.micro provides 1 GB RAM and 1 vCPU (suitable for light workloads)
- Instance will incur charges outside AWS Free Tier limits

## 🧹 Cleanup

Always run `terraform destroy` when resources are no longer needed to avoid unnecessary AWS charges. This will remove:
- EC2 instance
- SSH key pair
- All associated resources created by this configuration